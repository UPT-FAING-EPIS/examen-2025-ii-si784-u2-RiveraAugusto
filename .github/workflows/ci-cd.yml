name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Debug - List directory contents
      run: |
        echo "Root directory contents:"
        ls -la
        echo "Ejercicio 1 directory contents:"
        ls -la "Ejercicio 1/"
        echo "Checking if solution file exists:"
        ls -la "Ejercicio 1/TransportSolution.sln"
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'
    
    - name: Restore dependencies
      run: |
        cd "Ejercicio 1"
        dotnet restore TransportSolution.sln
    
    - name: Build
      run: |
        cd "Ejercicio 1"
        dotnet build TransportSolution.sln --no-restore
    
    - name: Run Unit Tests
      run: |
        cd "Ejercicio 1"
        dotnet test TransportLib.Tests --no-build --verbosity normal --collect:"XPlat Code Coverage" --results-directory ./TestResults
    
    - name: Run BDD Tests
      run: |
        cd "Ejercicio 1"
        dotnet test TransportLib.Specs --no-build --verbosity normal
    
    - name: Install ReportGenerator
      run: dotnet tool install -g dotnet-reportgenerator-globaltool
    
    - name: Generate Coverage Report
      run: |
        cd "Ejercicio 1"
        reportgenerator -reports:"./TestResults/**/coverage.cobertura.xml" -targetdir:"./CoverageReport" -reporttypes:Html
    
    - name: Upload Coverage Report
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: 'Ejercicio 1/CoverageReport'
    
    - name: Deploy to GitHub Pages
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: 'Ejercicio 1/CoverageReport'
        destination_dir: coverage
    
    - name: Comment PR with Coverage
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const fs = require('fs');
          const path = 'Ejercicio 1/CoverageReport/index.html';
          if (fs.existsSync(path)) {
            const coverage = 'Coverage report generated successfully! 📊';
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## Test Results\n\n✅ All tests passed!\n\n${coverage}\n\nView the full coverage report in the artifacts.`
            });
          }